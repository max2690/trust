# 🔧 Настройка Telegram бота на Vercel через Webhook

## ✅ Что было сделано

1. ✅ Создан webhook endpoint: `/api/telegram/webhook`
2. ✅ Создан setup endpoint: `/api/telegram/setup` (автоматическая настройка webhook)
3. ✅ Создан модуль обработки webhook: `src/lib/telegram-webhook-handler.ts`
4. ✅ Изменен `telegram-init.ts` - polling отключен на Vercel

## 🚀 Инструкция по настройке

### Шаг 1: Закоммитьте и запушьте изменения

```bash
cd mb-trust
git add .
git commit -m "Add Telegram webhook support for Vercel"
git push origin main
```

### Шаг 2: Дождитесь деплоя на Vercel

После успешного деплоя скопируйте URL вашего приложения (например: `https://your-app.vercel.app`)

### Шаг 3: Настройте webhook автоматически

Откройте в браузере:
```
https://your-app.vercel.app/api/telegram/setup
```

Это автоматически установит webhook на ваш Telegram бот.

**Или вручную через curl:**
```bash
curl -X GET "https://your-app.vercel.app/api/telegram/setup"
```

### Шаг 4: Проверьте webhook

Откройте в браузере:
```
https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
```

Замените `YOUR_BOT_TOKEN` на ваш токен бота.

Должно вернуть:
```json
{
  "ok": true,
  "result": {
    "url": "https://your-app.vercel.app/api/telegram/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0
  }
}
```

### Шаг 5: Проверьте работу бота

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Бот должен ответить

## 🔍 Проверка логов

Если бот не работает, проверьте логи в Vercel:
1. Vercel Dashboard → Ваш проект → Deployments
2. Выберите последний деплой → View Function Logs
3. Найдите логи с префиксом `[BOT]` или `[WEBHOOK]`

## ⚠️ Важно

1. **Переменные окружения должны быть установлены в Vercel:**
   - `TELEGRAM_BOT_TOKEN` - токен бота
   - `NEXTAUTH_URL` - URL вашего приложения (например: `https://your-app.vercel.app`)
   - `OPENAI_API_KEY` - для AI функций (опционально)

2. **После каждого деплоя webhook остается активным** - не нужно настраивать заново

3. **Если webhook не работает:**
   - Проверьте, что `NEXTAUTH_URL` установлен правильно
   - Убедитесь, что endpoint `/api/telegram/webhook` доступен (попробуйте открыть в браузере - должен вернуть JSON)
   - Проверьте логи в Vercel

## 📝 Структура файлов

```
src/
├── app/
│   └── api/
│       └── telegram/
│           ├── webhook/
│           │   └── route.ts      # Webhook endpoint
│           └── setup/
│               └── route.ts      # Setup endpoint
└── lib/
    ├── telegram-init.ts          # Инициализация бота (polling для локальной разработки)
    └── telegram-webhook-handler.ts  # Обработка webhook updates
```

## 🎯 Как это работает

1. **На Vercel (production):**
   - Polling отключен (serverless не поддерживает)
   - Telegram отправляет обновления на `/api/telegram/webhook`
   - Webhook handler обрабатывает сообщения

2. **Локально (development):**
   - Polling включен (для удобства разработки)
   - Можно тестировать без настройки webhook

## ✅ Готово!

После выполнения всех шагов ваш Telegram бот будет работать на Vercel через webhook.
