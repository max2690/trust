# ⚡ БЫСТРАЯ УСТАНОВКА ЗАЩИТЫ БОТА

## 📦 Файлы для копирования

```bash
# 1. На локальном компьютере выполните:
scp src/lib/bot-launcher.ts root@109.69.58.185:/var/www/mb-trust/src/lib/
scp src/lib/telegram-init.ts root@109.69.58.185:/var/www/mb-trust/src/lib/
scp mb-trust-production.service root@109.69.58.185:/tmp/
```

## 🔧 На сервере

```bash
# 2. Подключитесь к серверу
ssh root@109.69.58.185

# 3. Установите systemd unit
sudo cp /tmp/mb-trust-production.service /etc/systemd/system/mb-trust.service
sudo systemctl daemon-reload

# 4. Пересоберите проект
cd /var/www/mb-trust
npm run build

# 5. Перезапустите сервис
sudo systemctl restart mb-trust

# 6. Проверьте логи
journalctl -u mb-trust -f
```

## ✅ Проверка успеха

**В логах должно быть:**
```
🤖 Запуск Telegram бота...
✅ Telegram бот успешно запущен
```

**Если НЕТ этих строк:**
```bash
curl http://localhost:3000/api/health
journalctl -u mb-trust -n 20
```

---

## 🎯 Что изменилось

### `src/lib/bot-launcher.ts`
- ✅ Защита от конфликтов 409
- ✅ Проверка живости бота каждые 2 минуты
- ✅ Graceful shutdown
- ✅ Автоперезапуск при сбоях

### `src/lib/telegram-init.ts`
- ✅ Rate limiting: макс. 10 сообщений/минуту
- ✅ Валидация: блокировка спама, длинных сообщений, рекламы
- ✅ Автоочистка сессий через 30 минут неактивности
- ✅ OpenAI retry: 3 попытки с экспоненциальной задержкой
- ✅ Fallback на простой парсер если OpenAI не работает

### `mb-trust-production.service`
- ✅ Автозапуск бота через health check
- ✅ Restart=always для автоперезапуска
- ✅ Graceful stop за 10 секунд

---

## 🧪 Быстрый тест

```bash
# 1. Проверка автозапуска
sudo systemctl restart mb-trust && sleep 5 && journalctl -u mb-trust | grep "Telegram бот успешно запущен"

# 2. Тест регистрации
# Откройте http://109.69.58.185:3000 и пройдите регистрацию

# 3. Тест спама (отправьте боту 15 сообщений подряд)
# Ожидается: 11-15 сообщения заблокированы

# 4. Проверка статистики
journalctl -u mb-trust | grep -E "SPAM|CLEANUP" | tail -20
```

---

## 🆘 Экстренная помощь

```bash
# Бот не запускается?
curl http://localhost:3000/api/health

# 409 конфликт?
sudo systemctl stop mb-trust && sleep 5 && sudo systemctl start mb-trust

# Ошибки сборки?
sudo chown -R www-data:www-data /var/www/mb-trust/.next
npm run build
```

---

**Подробная документация:**
- `INSTALLATION.md` — полная инструкция с тестами
- `PROTECTION_GUIDE.md` — описание всех защит

