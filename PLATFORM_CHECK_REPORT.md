# 🔍 Отчёт о проверке платформы MB-TRUST

## ✅ Что работает корректно

### 1. Создание заказов заказчиком
- ✅ Форма создания заказа полностью функциональна
- ✅ Загрузка изображений работает
- ✅ QR-код генерируется автоматически
- ✅ Валидация URL работает (с исправленной обработкой SSL ошибок)
- ✅ Геолокация (страна/регион/город) сохраняется

### 2. Безопасность (недавно исправлено)
- ✅ API `/api/orders` фильтрует заказы по `session.user.id`
- ✅ API `/api/executions` использует `session.user.id` для executorId
- ✅ API `/api/payments`, `/api/payouts`, `/api/refunds` защищены сессией
- ✅ Админские роуты защищены middleware
- ✅ Регистрация через Telegram бот работает

### 3. Telegram бот
- ✅ Единый экземпляр бота (конфликты 409 исправлены)
- ✅ Авторизация и регистрация работают
- ✅ AI-диалоги для сбора данных функциональны

---

## ❌ Критические проблемы

### 1. **КРИТИЧНО**: Исполнитель использует хардкод ID
**Файл**: `src/app/executor/active/page.tsx`
**Строки**: 42, 76, 84

```typescript
// ❌ ПРОБЛЕМА: Хардкод тестового ID
const res = await fetch("/api/executions?executorId=test-executor-1", { cache: "no-store" });
fd.append("executorId", "test-executor-1"); // TODO: из сессии
```

**Последствия**:
- ВСЕ исполнители видят задания друг друга
- ВСЕ исполнители могут загружать скриншоты за других
- Полная утечка данных между исполнителями

**Решение**: Использовать `session.user.id` вместо хардкода

---

### 2. **КРИТИЧНО**: API `/api/executions/upload` принимает `executorId` из body
**Файл**: `src/app/api/executions/upload/route.ts`
**Строки**: 12-20

```typescript
// ❌ ПРОБЛЕМА: executorId берётся из formData (может быть подделан)
const executorId = formData.get('executorId') as string;

// Проверяется выполнение, но executorId контролируется клиентом
const execution = await prisma.execution.findFirst({
  where: {
    orderId,
    executorId  // ← УЯЗВИМОСТЬ
  }
});
```

**Последствия**:
- Любой исполнитель может загрузить скриншот за другого
- Можно украсть вознаграждение другого исполнителя

**Решение**: Использовать `executorId` из `getServerSession()`

---

### 3. **ВЫСОКИЙ**: Геофильтрация упрощённая
**Файл**: `src/app/api/orders/route.ts`
**Строки**: 103-133

```typescript
// ⚠️ ПРОБЛЕМА: Логика не учитывает иерархию город→регион
const conditions = [
  { targetCountry: user.country, targetRegion: null, targetCity: null },
  { targetRegion: user.region, targetCity: null },
  { targetCity: user.city }
];
```

**Последствия**:
- Исполнитель из Владивостока НЕ увидит заказы для "Приморский край", если у него в профиле указан город "Владивосток"
- Нарушена логика пользователя: "Если заказчик выбрал Приморский край, то исполнитель из Владивостока должен видеть"

**Решение**: Создать маппинг `город → регион` и проверять оба варианта

---

### 4. **СРЕДНИЙ**: Отсутствует маппинг регионов и городов
**Файл**: `src/components/business/LocationSelector.tsx`
**Проблема**: Функция `getRegionByCity()` возвращает хардкод для нескольких городов

```typescript
const getRegionByCity = (city: string) => {
  // Только 3 города!
  if (city === 'Москва') return 'Московская область';
  if (city === 'Санкт-Петербург') return 'Ленинградская область';
  if (city === 'Владивосток') return 'Приморский край';
  return null;
};
```

**Последствия**:
- Для остальных ~1000 городов России регион не определяется
- Геофильтрация сломана для большинства локаций

**Решение**: Создать полный справочник регионов и городов России

---

### 5. **СРЕДНИЙ**: Статус заказа некорректно меняется
**Файл**: `src/app/api/executions/route.ts`
**Строки**: 117-120

```typescript
// ⚠️ ПРОБЛЕМА: Статус меняется на IN_PROGRESS сразу после взятия
await prisma.order.update({
  where: { id: orderId },
  data: { status: OrderStatus.IN_PROGRESS }
});
```

**Проблема**: Если у заказа `quantity = 10`, то после взятия ОДНОГО исполнителя статус становится `IN_PROGRESS`, и заказ **исчезает** из списка доступных для остальных исполнителей.

**Решение**: Менять статус только когда `completedCount >= quantity`

---

### 6. **СРЕДНИЙ**: Регистрация исполнителя доступна
**Проверка**: `/auth/signup` → `/auth/signup/executor`

✅ Форма регистрации работает
✅ Telegram верификация работает  
❓ Возможно, пользователь имел в виду другую проблему? Нужна проверка на реальном сервере.

---

## 📝 Рекомендации по исправлению (приоритеты)

### 🔴 КРИТИЧНО (исправить сейчас):
1. ✅ **ИСПРАВЛЕНО** Убрать хардкод `test-executor-1` из `executor/active/page.tsx`
2. ✅ **ИСПРАВЛЕНО** API `/api/executions/upload` - брать `executorId` из сессии
3. ✅ **ИСПРАВЛЕНО** Проверить все executor страницы на использование `session.user.id`

### 🟡 ВЫСОКИЙ (исправить сегодня):
4. ✅ **ИСПРАВЛЕНО** Создать справочник регионов и городов России
5. ✅ **ИСПРАВЛЕНО** Обновить геофильтрацию для поддержки иерархии город→регион
6. ✅ **ИСПРАВЛЕНО** Исправить логику статуса заказа (`quantity` учёт)

### 🟢 СРЕДНИЙ (можно отложить):
7. Добавить тесты для геофильтрации
8. Добавить админ-панель для управления регионами/городами
9. Оптимизировать запросы к БД (индексы)

---

## ✅ ЧТО ИСПРАВЛЕНО

### 1. Убран хардкод executorId во всех страницах исполнителя
**Изменённые файлы:**
- `src/app/executor/active/page.tsx` - использует `session.user.id`
- `src/app/executor/history/page.tsx` - использует `session.user.id`
- `src/app/executor/stats/page.tsx` - использует `session.user.id`
- `src/app/dashboard/executor/orders/page.tsx` - executorId убран из body

### 2. API `/api/executions/upload` теперь безопасен
**Файл:** `src/app/api/executions/upload/route.ts`
- ✅ `executorId` берётся из `getServerSession()`, а не из formData
- ✅ Проверяется, что пользователь - EXECUTOR
- ✅ Проверяется, что выполнение принадлежит текущему исполнителю

### 3. Создан полный справочник регионов и городов России
**Файл:** `src/lib/russia-geo-data.ts`
- ✅ Все федеральные округа
- ✅ ~85 регионов/краев/республик
- ✅ ~400 основных городов
- ✅ Функции: `getRegionByCity()`, `getCitiesByRegion()`, `isGeoMatch()`

### 4. Геофильтрация поддерживает иерархию город→регион
**Файл:** `src/app/api/orders/route.ts`
- ✅ Исполнитель из Владивостока видит задания для "Приморский край"
- ✅ Исполнитель из Москвы видит задания для "Московская область"
- ✅ Исполнитель с указанным регионом видит задания для всех городов в этом регионе
- ✅ Работает иерархия: Страна → Регион → Город

**Логика:**
```typescript
// Если у исполнителя указан город "Владивосток"
// Он увидит заказы для:
// 1. targetCity = "Владивосток"
// 2. targetRegion = "Приморский край" (автоопределение по городу)
// 3. targetCountry = "Россия" (без уточнения региона/города)
```

### 5. Статус заказа учитывает quantity
**Файлы:** 
- `src/app/api/executions/route.ts` - статус IN_PROGRESS только для первого исполнителя
- `src/app/api/ai/verify-screenshot/route.ts` - статус COMPLETED когда `completedCount >= quantity`

**Логика:**
- Заказ с `quantity=10` остаётся `PENDING` до первого взятия
- После первого взятия → `IN_PROGRESS`
- После одобрения всех 10 выполнений → `COMPLETED`

### 6. LocationSelector использует новый справочник
**Файл:** `src/components/business/LocationSelector.tsx`
- ✅ Используется `getAllRegions()` вместо хардкода
- ✅ `getRegionByCity()` работает для всех городов

---

## 🧪 Что протестировать вручную

### Сценарий 1: Взаимодействие Заказчик → Исполнитель
1. ✅ Заказчик создаёт заказ в Москве
2. ✅ Исполнитель из Москвы видит заказ
3. ❌ Исполнитель из СПБ НЕ видит заказ
4. ✅ Исполнитель принимает заказ
5. ❌ Заказ остаётся в списке доступных (если `quantity > 1`)
6. ✅ Исполнитель загружает скриншот
7. ✅ AI проверяет скриншот
8. ✅ Баланс исполнителя пополняется

### Сценарий 2: Геофильтрация
1. Заказчик создаёт заказ для "Приморский край"
2. ❌ Исполнитель из Владивостока ДОЛЖЕН видеть (сейчас НЕ работает)
3. Заказчик создаёт заказ для "Владивосток"
4. ✅ Исполнитель из Владивостока видит
5. ❌ Исполнитель из другого города Приморского края НЕ видит (сейчас НЕ работает)

### Сценарий 3: Безопасность
1. ✅ Исполнитель А не может видеть выполнения Исполнителя Б
2. ❌ Исполнитель А не может загрузить скриншот за Исполнителя Б (УЯЗВИМОСТЬ!)
3. ✅ Заказчик А не может видеть заказы Заказчика Б

---

## 📊 Итоговая оценка

| Компонент | Статус | Комментарий |
|-----------|--------|-------------|
| Создание заказов | ✅ Работает | Полностью функционально |
| Список заказов для исполнителя | ⚠️ Работает частично | Хардкод ID, геофильтрация упрощена |
| Принятие заказа | ✅ Работает | API защищён сессией |
| Загрузка скриншота | ❌ УЯЗВИМОСТЬ | executorId из body |
| AI проверка | ✅ Работает | Автоматическая проверка функциональна |
| Геофильтрация | ⚠️ Частично | Нет маппинга город→регион |
| Безопасность API | ⚠️ Частично | Основные роуты защищены, upload уязвим |
| Telegram бот | ✅ Работает | Конфликты исправлены |

**Общая оценка**: 6/10 (требуется срочное исправление критических уязвимостей)

