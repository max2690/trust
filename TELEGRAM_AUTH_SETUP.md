# Telegram Авторизация - Инструкция по настройке

## 🎯 Что было реализовано

Добавлена полноценная авторизация через Telegram с использованием Telegram Login Widget. Если у пользователя привязан Telegram ID к аккаунту, он может войти в систему одним кликом через Telegram.

## 📁 Измененные файлы

### Новые файлы:
1. `src/app/api/auth/telegram/route.ts` - API endpoint для обработки Telegram OAuth
2. `src/components/auth/TelegramLoginButton.tsx` - Компонент кнопки Telegram авторизации

### Обновленные файлы:
3. `src/lib/auth.ts` - Добавлен Telegram provider в NextAuth
4. `src/app/auth/signin/page.tsx` - Обновлена страница входа с Telegram Login Widget
5. `next.config.ts` - Добавлена публичная переменная окружения для имени бота

## ⚙️ Настройка переменных окружения

Добавьте в ваш `.env.local` файл:

```env
# Telegram Bot (эти переменные уже должны быть у вас)
TELEGRAM_BOT_TOKEN="8345454942:AAFoOh3asQ104UUvIxNgMTpLCwwFI1oIJDY"
TELEGRAM_BOT_USERNAME="MBTRUST_bot"

# НОВАЯ переменная для клиентской части
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME="MBTRUST_bot"
```

## 🔐 Как работает авторизация

### 1. Пользователь нажимает кнопку "Telegram" на странице входа

### 2. Загружается Telegram Login Widget
- Виджет создается через официальный скрипт Telegram
- URL: `https://telegram.org/js/telegram-widget.js`
- Конфигурация включает имя бота и параметры безопасности

### 3. Пользователь авторизуется через Telegram
- Открывается всплывающее окно Telegram
- Пользователь подтверждает авторизацию
- Telegram возвращает данные пользователя с хешем для проверки

### 4. Данные проверяются на сервере
- Проверяется подлинность данных от Telegram (проверка хеша)
- Проверяется, что данные не устарели (не старше 24 часов)
- Ищется пользователь с таким `telegramId` в базе данных

### 5. Создается сессия пользователя
- Если пользователь найден и не заблокирован - создается сессия через NextAuth
- Пользователь перенаправляется на нужную страницу (dashboard/executor)
- Если пользователь не найден - показывается ошибка с предложением зарегистрироваться

## 🔧 Проверка подлинности данных

Реализована полная проверка согласно официальной документации Telegram:
https://core.telegram.org/widgets/login#checking-authorization

```typescript
// Шаги проверки:
1. Извлекаем hash из данных
2. Создаем строку для проверки из остальных данных
3. Создаем секретный ключ = SHA256(bot_token)
4. Создаем хеш = HMAC_SHA256(secret_key, data_check_string)
5. Сравниваем полученный хеш с hash от Telegram
```

## 📊 База данных

Используются существующие поля в таблице `User`:
- `telegramId` - Telegram ID пользователя (уникальный)
- `telegramUsername` - @username в Telegram (обновляется при каждом входе)

## 🚀 Деплой на сервер

### 1. Загрузите файлы на сервер

```bash
cd /var/www/mb-trust

# Загрузите все измененные файлы
```

### 2. Добавьте переменную окружения

```bash
# Отредактируйте .env.local
nano .env.local

# Добавьте строку:
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME="MBTRUST_bot"
```

### 3. Пересоберите проект

```bash
npm ci
npx prisma generate
npm run build
```

### 4. Перезапустите сервис

```bash
sudo systemctl restart mb-trust
```

### 5. Проверьте работу

```bash
# Проверьте логи
journalctl -u mb-trust -f

# Откройте страницу входа
# http://109.69.58.185:3000/auth/signin
```

## ✅ Тестирование

### Сценарий 1: Успешный вход
1. Откройте `/auth/signin`
2. Нажмите на кнопку Telegram Login Widget
3. Подтвердите авторизацию в Telegram
4. Вы должны быть автоматически перенаправлены на dashboard

### Сценарий 2: Telegram не привязан
1. Откройте `/auth/signin`
2. Нажмите на кнопку Telegram
3. Подтвердите авторизацию
4. Должно появиться сообщение: "Telegram аккаунт не привязан к учетной записи"

## 🔍 Диагностика проблем

### Проблема: Кнопка Telegram не появляется

**Проверьте:**
```bash
# Переменная окружения установлена?
cat .env.local | grep NEXT_PUBLIC_TELEGRAM_BOT_USERNAME

# Пересобран ли проект?
ls -la .next/
```

**Решение:**
- Добавьте переменную `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME` в `.env.local`
- Пересоберите проект: `npm run build`

### Проблема: Ошибка "invalid_auth"

**Проверьте логи:**
```bash
journalctl -u mb-trust -f | grep "TELEGRAM AUTH"
```

**Возможные причины:**
- Неправильный `TELEGRAM_BOT_TOKEN`
- Кто-то пытается подделать данные от Telegram

### Проблема: Ошибка "user_not_found"

**Это нормальное поведение!**
Пользователь еще не привязал Telegram к аккаунту.

**Решение для пользователя:**
1. Зарегистрироваться через форму регистрации
2. При регистрации пройти верификацию через Telegram бота
3. После этого можно будет входить через Telegram

## 📋 Логи для отладки

При авторизации через Telegram в логах будут строки:

```bash
# Получение данных от Telegram
[TELEGRAM AUTH] Получены данные: {id: "123456789", username: "user"}

# Проверка подлинности
[TELEGRAM AUTH] Проверка подлинности не прошла  # Ошибка
# или
# Проверка прошла успешно (нет сообщения = успех)

# Поиск пользователя
[TELEGRAM AUTH] Пользователь с telegramId не найден: 123456789
# или
[TELEGRAM AUTH] Успешная авторизация: {userId: "...", name: "...", role: "..."}
```

## 🔗 Связь с существующей системой

Telegram авторизация полностью интегрирована с существующей системой:

1. **Регистрация через бота** - при регистрации через Telegram бота устанавливается `telegramId`
2. **Авторизация** - пользователь может войти через Telegram или через форму (телефон + пароль)
3. **Безопасность** - все проверки подлинности и блокировки пользователей работают

## 📝 Чеклист после деплоя

- [ ] Переменная `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME` добавлена в `.env.local`
- [ ] Проект пересобран (`npm run build`)
- [ ] Сервис перезапущен (`systemctl restart mb-trust`)
- [ ] Кнопка Telegram появляется на странице `/auth/signin`
- [ ] При клике открывается Telegram авторизация
- [ ] Пользователь с привязанным Telegram может войти
- [ ] Пользователь без привязанного Telegram видит корректную ошибку

## 🎨 Особенности UI

- Кнопка Telegram использует официальный виджет Telegram
- Стиль кнопки настраивается через параметры виджета:
  - `data-size="large"` - большой размер
  - `data-radius="8"` - скругленные углы
- При загрузке показывается текст "Авторизация..."
- При ошибке показывается понятное сообщение

## 🔐 Безопасность

✅ **Реализовано:**
- Полная проверка подлинности данных от Telegram (HMAC-SHA256)
- Проверка времени авторизации (не старше 24 часов)
- Проверка блокировки пользователя
- Защита от CSRF через NextAuth
- Безопасное хранение токенов в JWT

✅ **Не требуется хранить:**
- Telegram token в клиентском коде
- Пользовательские данные в localStorage
- Пароли пользователей (используется Telegram)

## 📖 Дополнительная информация

- [Telegram Login Widget документация](https://core.telegram.org/widgets/login)
- [NextAuth.js документация](https://next-auth.js.org/)
- [Checking Telegram Authorization](https://core.telegram.org/widgets/login#checking-authorization)

