# 🔐 Настройка переменных окружения

## Обязательные переменные для безопасности

### 1. INTERNAL_API_SECRET
**Где используется:**
- `src/lib/telegram-init.ts` - бот обращается к `/api/users` с этим заголовком
- `src/app/api/users/route.ts` - проверяет заголовок `x-internal-secret`

**Как настроить:**
```bash
# На сервере: /var/www/mb-trust/.env
INTERNAL_API_SECRET=ваш_случайный_секрет_минимум_32_символа

# Пример генерации безопасного секрета:
openssl rand -hex 32
# или
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**Зачем нужен:**
- Telegram бот работает на том же сервере, но не имеет доступа к сессиям пользователей
- Для внутренних запросов бота к API используется этот секрет
- Защищает `/api/users` от публичного доступа, разрешая только внутренние вызовы

---

### 2. NEXTAUTH_SECRET
```bash
NEXTAUTH_SECRET=ваш_nextauth_секрет_минимум_32_символа
```

Используется для подписи JWT токенов NextAuth.

---

### 3. TELEGRAM_BOT_TOKEN
```bash
TELEGRAM_BOT_TOKEN=123456:ABC-DEF-GHI...
```

Токен бота из BotFather.

---

### 4. OPENAI_API_KEY (опционально)
```bash
OPENAI_API_KEY=sk-...
```

Для AI проверки скриншотов. Если не указан, используется заглушка.

---

## 📋 Чеклист после развёртывания

1. ✅ Создайте `.env` файл на сервере
2. ✅ Добавьте `INTERNAL_API_SECRET` (сгенерируйте новый!)
3. ✅ Перезапустите сервис: `sudo systemctl restart mb-trust`
4. ✅ Проверьте логи: `journalctl -u mb-trust -f`
5. ✅ Убедитесь, что нет ошибок 409 (Telegram конфликт)
6. ✅ Протестируйте регистрацию исполнителя
7. ✅ Протестируйте взятие заказа и загрузку скриншота

---

## ⚠️ Важно

- **НЕ коммитьте** `.env` файл в git
- **Используйте разные секреты** для dev и production
- **Обновите секреты** после первого развёртывания

