# 📱 ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## Мобильное приложение MB-TRUST

**Версия:** 1.0  
**Дата создания:** 2024  
**Проект:** MB-TRUST Mobile App

---

## 🎯 1. ОБЩЕЕ ОПИСАНИЕ

### 1.1 Назначение приложения
Мобильное приложение MB-TRUST для платформы Android и iOS, позволяющее пользователям:
- **Исполнителям**: находить и выполнять задания из социальных сетей
- **Заказчикам**: создавать задания и управлять рекламными кампаниями
- **Всем пользователям**: управлять балансом, получать выплаты, отслеживать статистику

### 1.2 Целевая аудитория
- **Основная**: Исполнители (люди с аккаунтами в Instagram, TikTok, VK, Telegram)
- **Вторичная**: Заказчики (бизнес, рекламодатели)
- **Дополнительная**: Администраторы (опционально, упрощенная версия)

### 1.3 Платформы
- ✅ **Android** 6.0+ (API Level 23+)
- ✅ **iOS** 12.0+

---

## 🏗️ 2. АРХИТЕКТУРА И ТЕХНОЛОГИИ

### 2.1 Выбор технологического стека

**Рекомендуемая технология: React Native (Expo)**

**Обоснование выбора:**
- ✅ **Кроссплатформенность**: Один код для Android и iOS
- ✅ **Быстрая разработка**: Готовая инфраструктура, hot reload
- ✅ **Переиспользование кода**: Логика похожа на веб-версию (Next.js)
- ✅ **Богатая экосистема**: Много готовых библиотек
- ✅ **OTA обновления**: Обновления без публикации в магазинах
- ✅ **Простота деплоя**: Expo делает сборку простой

**Альтернативные варианты:**
- Flutter (если нужен нативный UI и высокая производительность)
- Нативный Android (Kotlin) + iOS (Swift) (если критична производительность)

### 2.2 Структура проекта

```
mb-trust-mobile/
├── src/
│   ├── screens/                    # Все экраны приложения
│   │   ├── auth/                   # Авторизация и регистрация
│   │   │   ├── LoginScreen.tsx
│   │   │   ├── RegisterScreen.tsx
│   │   │   └── VerificationScreen.tsx
│   │   ├── executor/               # Панель исполнителя
│   │   │   ├── AvailableOrdersScreen.tsx
│   │   │   ├── ActiveOrdersScreen.tsx
│   │   │   └── HistoryScreen.tsx
│   │   ├── customer/               # Панель заказчика
│   │   │   ├── CreateOrderScreen.tsx
│   │   │   ├── MyOrdersScreen.tsx
│   │   │   └── OrderDetailsScreen.tsx
│   │   ├── profile/                # Профиль пользователя
│   │   │   ├── ProfileScreen.tsx
│   │   │   ├── EditProfileScreen.tsx
│   │   │   └── SettingsScreen.tsx
│   │   ├── payments/               # Платежи и выплаты
│   │   │   ├── BalanceScreen.tsx
│   │   │   ├── DepositScreen.tsx
│   │   │   └── PayoutScreen.tsx
│   │   └── stats/                  # Статистика
│   │       └── StatisticsScreen.tsx
│   ├── components/                 # React компоненты
│   │   ├── ui/                     # UI компоненты (кнопки, карточки, формы)
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Input.tsx
│   │   │   └── Badge.tsx
│   │   └── business/               # Бизнес-компоненты
│   │       ├── OrderCard.tsx
│   │       ├── ExecutionCard.tsx
│   │       └── BalanceCard.tsx
│   ├── services/                   # API сервисы и бизнес-логика
│   │   ├── api.ts                  # Базовый API клиент
│   │   ├── auth.ts                 # Аутентификация
│   │   ├── orders.ts               # Работа с заказами
│   │   ├── payments.ts             # Платежи и выплаты
│   │   ├── profile.ts              # Профиль пользователя
│   │   └── verification.ts         # Верификация
│   ├── navigation/                 # Навигация
│   │   ├── AppNavigator.tsx        # Главный навигатор
│   │   ├── AuthNavigator.tsx       # Навигация для неавторизованных
│   │   └── MainNavigator.tsx       # Навигация для авторизованных
│   ├── store/                      # State Management
│   │   ├── authStore.ts            # Хранение данных авторизации
│   │   ├── ordersStore.ts          # Кэш заказов
│   │   └── profileStore.ts         # Профиль пользователя
│   ├── hooks/                      # Custom React hooks
│   │   ├── useAuth.ts
│   │   ├── useOrders.ts
│   │   └── usePayments.ts
│   ├── utils/                      # Утилиты
│   │   ├── storage.ts              # AsyncStorage обёртка
│   │   ├── formatting.ts           # Форматирование данных
│   │   └── validation.ts          # Валидация форм
│   └── types/                      # TypeScript типы
│       ├── auth.types.ts
│       ├── order.types.ts
│       └── user.types.ts
├── assets/                         # Ресурсы
│   ├── images/                     # Изображения
│   ├── icons/                      # Иконки
│   └── fonts/                      # Шрифты
├── app.json                        # Expo конфигурация
├── package.json
├── tsconfig.json
└── .env.example                    # Пример переменных окружения
```

---

## 🔌 3. API ИНТЕГРАЦИЯ

### 3.1 Базовый принцип
Мобильное приложение является **клиентом** для существующего Next.js API. Все данные синхронизируются автоматически через API запросы.

**Base URL API:**
- **Development**: `http://localhost:3000/api` (для тестирования)
- **Production**: `https://your-domain.com/api` (после деплоя)

### 3.2 Аутентификация для мобильного приложения

#### Проблема:
Существующий API использует NextAuth с HTTP-only cookies, которые не работают в мобильных приложениях.

#### Решение:
Нужно добавить **JWT токен аутентификацию** для мобильного приложения.

**Что нужно сделать на бэкенде:**

1. **Создать endpoint для мобильного логина:**
   ```
   POST /api/auth/mobile/login
   Body: { phone: string, password: string }
   Response: { token: string, user: User }
   ```

2. **Добавить проверку Bearer токенов в API routes:**
   - Проверять заголовок `Authorization: Bearer <token>`
   - Если токен валидный - использовать userId из токена
   - Если нет токена - использовать NextAuth сессию (для веб)

3. **Создать endpoint для обновления токена:**
   ```
   POST /api/auth/mobile/refresh
   Headers: { Authorization: Bearer <token> }
   Response: { token: string }
   ```

#### Как будет работать в мобильном приложении:

```typescript
// 1. При логине получаем токен
const response = await api.post('/auth/mobile/login', {
  phone: '+79001234567',
  password: 'password123'
});
const { token, user } = response.data;

// 2. Сохраняем токен локально (AsyncStorage)
await AsyncStorage.setItem('authToken', token);

// 3. Добавляем токен во все запросы
api.defaults.headers.common['Authorization'] = `Bearer ${token}`;
```

### 3.3 Основные API Endpoints

**Аутентификация:**
- `POST /api/auth/mobile/login` - Вход (новый для мобильного)
- `POST /api/auth/mobile/refresh` - Обновление токена
- `POST /api/auth/mobile/logout` - Выход

**Пользователи:**
- `POST /api/users` - Регистрация нового пользователя
- `GET /api/profile` - Получение профиля текущего пользователя
- `PUT /api/profile/update` - Обновление профиля

**Заказы:**
- `GET /api/orders` - Список доступных заказов (с фильтрами)
- `GET /api/orders/[id]` - Детали конкретного заказа
- `POST /api/orders` - Создание нового заказа (для заказчиков)
- `POST /api/orders/calculator` - Калькулятор стоимости заказа

**Выполнения (для исполнителей):**
- `GET /api/executions` - Список выполнений текущего пользователя
- `POST /api/executions` - Принятие задания (создание выполнения)
- `POST /api/executions/upload` - Загрузка скриншота выполнения

**Платежи:**
- `GET /api/payments` - История платежей
- `POST /api/payments/create` - Создание платежа (пополнение баланса)

**Выплаты:**
- `GET /api/payouts` - История выплат
- `POST /api/payouts/request` - Запрос выплаты

**Верификация:**
- `POST /api/verification/telegram/start` - Начало Telegram верификации
- `POST /api/verification/telegram/complete` - Завершение Telegram верификации

**Статистика:**
- `GET /api/executor/stats` - Статистика исполнителя
- `GET /api/executor/limits` - Лимиты исполнителя

---

## 🔐 4. АУТЕНТИФИКАЦИЯ И РЕГИСТРАЦИЯ

### 4.1 Экран входа (Login Screen)

**Поля:**
- Телефон (маска ввода: +7 (XXX) XXX-XX-XX)
- Пароль (с возможностью показать/скрыть)
- Чекбокс "Запомнить меня" (опционально)

**Действия:**
- Кнопка "Войти"
- Ссылка "Забыли пароль?" (опционально, на будущее)
- Ссылка "Нет аккаунта? Зарегистрироваться"

**Логика:**
1. Валидация формы (телефон и пароль обязательны)
2. API запрос: `POST /api/auth/mobile/login`
3. Сохранение токена в AsyncStorage
4. Переход на главный экран приложения

**API Endpoint:**
```typescript
POST /api/auth/mobile/login
Body: {
  phone: string,
  password: string
}
Response: {
  token: string,
  user: {
    id: string,
    name: string,
    phone: string,
    email?: string,
    role: 'EXECUTOR' | 'CUSTOMER',
    level: 'NOVICE' | 'VERIFIED' | 'REFERRAL' | 'TOP',
    balance: number
  }
}
```

### 4.2 Экран регистрации (Register Screen)

**Шаг 1: Основная информация**
- Телефон (маска ввода)
- Email (опционально)
- Имя
- Пароль (с подтверждением)
- Выбор роли: Исполнитель / Заказчик

**Шаг 2: Локация**
- Страна (по умолчанию "Россия")
- Регион (выбор из списка)
- Город (выбор из списка или автокомплит)

**Шаг 3: Принятие условий**
- Чекбокс "Я принимаю пользовательское соглашение"
- Чекбокс "Я принимаю политику конфиденциальности"

**Логика:**
1. Валидация на каждом шаге
2. После последнего шага: API запрос `POST /api/users`
3. Автоматический вход после регистрации
4. Переход на экран верификации (если требуется)

**API Endpoint:**
```typescript
POST /api/users
Body: {
  phone: string,
  email?: string,
  name: string,
  password: string,
  role: 'EXECUTOR' | 'CUSTOMER',
  country: string,
  region: string,
  city?: string
}
Response: {
  id: string,
  phone: string,
  name: string,
  // ... остальные поля пользователя
}
```

### 4.3 Экран верификации (Verification Screen)

**Варианты верификации:**

#### 4.3.1 Telegram верификация (приоритет)
- Кнопка "Подключить Telegram"
- Открытие Telegram бота через deep link
- Автоматическая привязка после подтверждения в боте
- Индикатор статуса: "Ожидает подключения" → "Подключено"

#### 4.3.2 Email верификация
- Поле ввода email (если не указан при регистрации)
- Кнопка "Отправить код"
- Поле ввода кода подтверждения
- Кнопка "Подтвердить"
- Таймер повторной отправки (60 секунд)

#### 4.3.3 SMS верификация (опционально)
- Поле ввода телефона
- Кнопка "Отправить SMS"
- Поле ввода кода
- Кнопка "Подтвердить"

**Логика:**
- После успешной верификации обновляется статус пользователя
- Можно пропустить верификацию (с ограничениями в функционале)

**API Endpoints:**
```typescript
// Telegram верификация
POST /api/verification/telegram/start
Response: { verificationCode: string, botUrl: string }

POST /api/verification/telegram/complete
Body: { verificationCode: string }
Response: { success: boolean, telegramId: string }

// Email/SMS верификация
POST /api/auth/verify
Body: { email: string, code: string } // или { phone: string, code: string }
Response: { success: boolean }
```

---

## 📋 5. ФУНКЦИОНАЛ ДЛЯ ИСПОЛНИТЕЛЕЙ

### 5.1 Экран доступных заданий (Available Orders Screen)

**Основной функционал:**
- Список доступных заданий (карточки)
- Pull-to-refresh (обновление списка)
- Infinite scroll (подгрузка при прокрутке)

**Карточка задания содержит:**
- Соцсеть (иконка + название: Instagram, TikTok, VK, Telegram, etc.)
- Награда (сумма в рублях, крупным шрифтом)
- Срок выполнения (дедлайн)
- Локация (регион/город)
- Требования к публикации (краткое описание)
- Кнопка "Взять задание"

**Фильтры (внизу экрана или в модальном окне):**
- По соцсети (Instagram, TikTok, VK, Telegram, etc.)
- По сумме награды (от/до)
- По региону/городу
- По сроку выполнения

**Сортировка:**
- По дате (новые сначала)
- По сумме (больше → меньше)
- По сроку выполнения (срочные сначала)

**Детали задания (при нажатии на карточку):**
- Полное описание задания
- Требования к контенту
- QR-код для публикации (после принятия)
- Кнопка "Взять задание"

**API:**
```typescript
GET /api/orders?status=PENDING&role=EXECUTOR&socialNetwork=INSTAGRAM&minReward=100
Response: {
  orders: Order[],
  total: number,
  page: number
}
```

### 5.2 Экран активных заданий (Active Orders Screen)

**Список заданий, которые исполнитель принял:**

**Для каждого задания показывается:**
- Название/описание задания
- Соцсеть
- Статус выполнения:
  - `PENDING` - Принято, нужно выполнить
  - `IN_PROGRESS` - В процессе (загружен скриншот, ожидает модерации)
  - `COMPLETED` - Выполнено и оплачено ✅
  - `REJECTED` - Отклонено модератором ❌
- Срок выполнения (таймер обратного отсчета или "Просрочено")
- Награда (сумма)
- QR-код (после принятия задания)
- Кнопка "Загрузить скриншот" (для статуса PENDING)

**При нажатии на задание:**
- Полная информация о задании
- Инструкции по выполнению
- Форма загрузки скриншота
- История статусов

**API:**
```typescript
GET /api/executions?executorId=current&status=IN_PROGRESS
Response: Execution[]
```

### 5.3 Загрузка скриншота (Screenshot Upload)

**Экран загрузки скриншота:**

**Варианты:**
1. **Выбрать из галереи** - открыть галерею устройства
2. **Сделать фото** - открыть камеру
3. **Сканировать QR-код** - открыть QR-сканер (опционально)

**После выбора изображения:**
- Предпросмотр изображения
- Кроп/редактирование (опционально)
- Сжатие изображения (автоматически)
- Кнопка "Загрузить"
- Прогресс-бар загрузки

**Логика:**
1. Выбор/создание изображения
2. Сжатие до максимального размера (например, 2MB)
3. Загрузка через `POST /api/executions/upload`
4. Обновление статуса задания на `IN_PROGRESS`
5. Возврат на экран активных заданий

**API:**
```typescript
POST /api/executions/upload
Headers: { Authorization: Bearer <token>, Content-Type: multipart/form-data }
Body: FormData {
  executionId: string,
  screenshot: File
}
Response: {
  success: boolean,
  execution: Execution
}
```

### 5.4 Экран истории заданий (History Screen)

**Список всех завершенных заданий:**
- Выполненные (`COMPLETED`) - с зеленой галочкой
- Отклоненные (`REJECTED`) - с красным крестиком

**Для каждого задания:**
- Дата выполнения
- Соцсеть
- Награда (если оплачено)
- Статус
- Причина отклонения (если отклонено)

**Статистика вверху экрана:**
- Всего выполнено: X заданий
- Заработано: XXX₽
- Средний рейтинг: ⭐ X.X

**Фильтры:**
- По статусу (выполненные/отклоненные)
- По периоду (сегодня, неделя, месяц, все время)
- По соцсети

---

## 💼 6. ФУНКЦИОНАЛ ДЛЯ ЗАКАЗЧИКОВ

### 6.1 Экран создания заказа (Create Order Screen)

**Форма создания заказа (многошаговая):**

**Шаг 1: Основная информация**
- Выбор соцсети (Instagram, TikTok, VK, Telegram, WhatsApp, Facebook)
- Количество публикаций (слайдер 1-100)
- Описание задания (текстовое поле, многострочное)
- Требования к контенту (чекбоксы или текст)

**Шаг 2: Локация и сроки**
- Выбор региона (где должны быть исполнители)
- Выбор города (опционально, можно "Все города")
- Дата начала выполнения
- Дедлайн (срок окончания)

**Шаг 3: Тип кампании**
- Одноразовая (разовое задание)
- Еженедельная (повторяется каждую неделю)
- Двухнедельная (повторяется каждые 2 недели)

**Шаг 4: Бюджет и калькулятор**
- Автоматический расчет стоимости через калькулятор
- Превью бюджета:
  - Цена за публикацию
  - Количество публикаций
  - **Общая стоимость** (крупным шрифтом)
  - Комиссия платформы (показывается отдельно)
- Кнопка "Создать заказ"

**Логика:**
1. При выборе соцсети и количества - автоматически вызывается калькулятор
2. Показывается предварительная стоимость
3. При подтверждении создается заказ через API
4. Переход на экран "Мои заказы"

**API:**
```typescript
// Калькулятор стоимости
POST /api/orders/calculator
Body: {
  platforms: [{ platform: 'INSTAGRAM', quantity: 50 }],
  campaignType: 'ONE_TIME' | 'WEEKLY' | 'BIWEEKLY',
  executionDays: number
}
Response: {
  totalCost: number,
  totalQuantity: number,
  platformCosts: { INSTAGRAM: 10000 },
  dailyDistribution: {}
}

// Создание заказа
POST /api/orders
Body: {
  socialNetwork: 'INSTAGRAM',
  quantity: 50,
  description: string,
  requirements: string,
  region: string,
  city?: string,
  startDate: string,
  deadline: string,
  campaignType: 'ONE_TIME',
  budget: number
}
Response: {
  id: string,
  // ... данные созданного заказа
}
```

### 6.2 Экран моих заказов (My Orders Screen)

**Список всех созданных заказчиком заказов:**

**Для каждого заказа показывается:**
- Соцсеть и количество публикаций
- Статус:
  - `PENDING` - Ожидает исполнителей
  - `IN_PROGRESS` - В процессе выполнения
  - `COMPLETED` - Завершен
  - `CANCELLED` - Отменен
- Прогресс выполнения:
  - "X из Y публикаций выполнено"
  - Прогресс-бар
- Общая сумма (бюджет заказа)
- Дата создания и дедлайн
- Количество принявших исполнителей
- Кнопка "Детали"

**Статистика вверху:**
- Всего создано: X заказов
- Потрачено: XXX₽
- Активных: X заказов

**Фильтры:**
- По статусу
- По соцсети
- По периоду

**API:**
```typescript
GET /api/orders?customerId=current
Response: {
  orders: Order[],
  total: number
}
```

### 6.3 Экран деталей заказа (Order Details Screen)

**Полная информация о заказе:**

**Вкладка "Информация":**
- Все данные заказа (описание, требования, сроки)
- Бюджет и комиссии
- Статус и прогресс

**Вкладка "Исполнители":**
- Список всех исполнителей, принявших задание
- Для каждого исполнителя:
  - Имя и уровень доверия
  - Статус выполнения его задания
  - Скриншот (если загружен)
  - Дата принятия задания
  - Кнопки действий:
    - "Принять" (если скриншот загружен)
    - "Отклонить" (с указанием причины)

**Вкладка "Статистика":**
- График выполнения по дням
- Количество кликов по QR-кодам
- Конверсия (если применимо)

**API:**
```typescript
GET /api/orders/[id]
Response: {
  order: Order,
  executions: Execution[],
  statistics: {}
}

// Модерация выполнения
PUT /api/moderate
Body: {
  executionId: string,
  action: 'APPROVE' | 'REJECT',
  reason?: string
}
```

---

## 💰 7. ПЛАТЕЖИ И ВЫПЛАТЫ

### 7.1 Экран баланса (Balance Screen)

**Для заказчиков:**
- Текущий баланс (крупным шрифтом)
- Кнопка "Пополнить баланс"
- История пополнений (последние 10)

**Для исполнителей:**
- Доступный баланс для вывода
- Заработано всего
- В обработке (сумма ожидающих выплату)
- Кнопка "Запросить выплату"
- История выплат

**API:**
```typescript
GET /api/profile
Response: {
  user: {
    balance: number,
    role: 'EXECUTOR' | 'CUSTOMER'
    // ...
  }
}
```

### 7.2 Экран пополнения баланса (Deposit Screen) - для заказчиков

**Форма пополнения:**
- Сумма пополнения (поле ввода)
- Кнопки быстрого выбора: 500₽, 1000₽, 2000₽, 5000₽
- Выбор метода оплаты:
  - Банковская карта (Visa, Mastercard, МИР)
  - YooKassa (если доступно)
  - Alfa-Bank (если доступно)
- Кнопка "Пополнить"

**Логика:**
1. Ввод суммы (минимум 100₽)
2. Выбор метода оплаты
3. API запрос: `POST /api/payments/create`
4. Открытие формы оплаты (встроенная или внешняя ссылка)
5. После успешной оплаты - обновление баланса

**API:**
```typescript
POST /api/payments/create
Body: {
  amount: number,
  method: 'YOOKASSA' | 'ALFA_BANK' | 'CARD',
  orderId?: string // если пополнение для конкретного заказа
}
Response: {
  paymentId: string,
  paymentUrl: string, // ссылка для оплаты
  amount: number
}

GET /api/payments?userId=current
Response: {
  payments: Payment[],
  total: number
}
```

### 7.3 Экран запроса выплаты (Payout Screen) - для исполнителей

**Форма запроса выплаты:**

**Доступный баланс:**
- Сумма доступна для вывода (крупным шрифтом)
- Минимальная сумма для выплаты: 100₽

**Сумма выплаты:**
- Поле ввода суммы (не больше доступного баланса)
- Кнопка "Вывести все"

**Метод выплаты:**
- Telegram Wallet (приоритет, если привязан)
- СБП (Система быстрых платежей) - по номеру телефона
- Банковская карта - по номеру карты

**Для Telegram Wallet:**
- Показывается привязанный Telegram ID
- Кнопка "Изменить" (если нужно)
- Кнопка "Вывести"

**Для СБП:**
- Поле ввода номера телефона (+7...)
- Кнопка "Подтвердить номер"
- После подтверждения - кнопка "Вывести"

**Для банковской карты:**
- Поле ввода номера карты (16 цифр)
- Поле ввода получателя (ФИО)
- Кнопка "Вывести"

**Логика:**
1. Ввод суммы и выбор метода
2. Валидация (сумма >= минимум, метод выбран)
3. API запрос: `POST /api/payouts/request`
4. Подтверждение запроса
5. Обновление баланса

**API:**
```typescript
POST /api/payouts/request
Body: {
  amount: number,
  method: 'TELEGRAM_WALLET' | 'SBP' | 'BANK_CARD',
  telegramWalletId?: string,
  sbpPhone?: string,
  cardNumber?: string,
  cardholderName?: string
}
Response: {
  payoutId: string,
  status: 'PENDING' | 'PROCESSING',
  amount: number,
  estimatedCompletion: string // дата завершения
}

GET /api/payouts?userId=current
Response: {
  payouts: Payout[],
  total: number
}
```

### 7.4 История транзакций

**Список всех операций:**
- Пополнения (для заказчиков)
- Выплаты (для исполнителей)
- Начисления за выполненные задания (для исполнителей)

**Для каждой транзакции:**
- Тип операции (иконка)
- Сумма (+ для пополнений/начислений, - для выплат)
- Дата и время
- Статус:
  - `COMPLETED` - Выполнено ✅
  - `PENDING` - В обработке ⏳
  - `REJECTED` - Отклонено ❌
- Метод платежа/выплаты

**Фильтры:**
- По типу (пополнения/выплаты/начисления)
- По статусу
- По периоду

---

## 👤 8. ПРОФИЛЬ И НАСТРОЙКИ

### 8.1 Экран профиля (Profile Screen)

**Информация о пользователе:**
- Фото профиля (аватар, опционально)
- Имя
- Телефон
- Email (если указан)
- Роль (Исполнитель / Заказчик)

**Для исполнителей дополнительно:**
- Уровень доверия (NOVICE → VERIFIED → REFERRAL → TOP)
  - Текущий уровень
  - Прогресс до следующего уровня (прогресс-бар)
  - Требования для повышения уровня
- Рейтинг (⭐ X.X из 5.0)
- Количество выполненных заданий
- Всего заработано
- Telegram (если привязан): "@username" или "Не привязан"

**Для заказчиков дополнительно:**
- Всего создано заказов
- Всего потрачено
- Средняя стоимость заказа

**Действия:**
- Кнопка "Редактировать профиль"
- Кнопка "Настройки"
- Кнопка "Выйти"

**API:**
```typescript
GET /api/profile
Response: {
  user: User,
  stats: {
    // статистика в зависимости от роли
  }
}
```

### 8.2 Экран редактирования профиля (Edit Profile Screen)

**Поля для редактирования:**
- Имя (обязательно)
- Email (опционально)
- Телефон (показывается, но не редактируется напрямую)
- Предпочитаемый мессенджер (Telegram / VK / WhatsApp)
- Примерное количество подписчиков (для исполнителей)
- Настройки уведомлений:
  - Push-уведомления (вкл/выкл)
  - Email уведомления
  - Ежедневные задания (для исполнителей)

**Кнопки:**
- "Сохранить изменения"
- "Отмена"

**API:**
```typescript
PUT /api/profile/update
Body: {
  name?: string,
  email?: string,
  preferredMessenger?: string,
  followersApprox?: number,
  dailyTasksOptIn?: boolean
}
Response: {
  success: boolean,
  user: User
}
```

### 8.3 Экран настроек (Settings Screen)

**Разделы настроек:**

**Уведомления:**
- Push-уведомления (переключатель)
- Email уведомления (переключатель)
- Telegram уведомления (если привязан Telegram)

**Безопасность:**
- Изменить пароль
- Вход по отпечатку/Face ID (опционально)

**Язык и регион:**
- Язык интерфейса (Русский / English)
- Часовой пояс

**О приложении:**
- Версия приложения
- Лицензионное соглашение
- Политика конфиденциальности
- Условия использования

**Прочее:**
- Очистить кэш
- Выйти из аккаунта

---

## 🎨 9. ДИЗАЙН И UI/UX

### 9.1 Дизайн-система

**Цветовая палитра:**
- **Основной фон**: Темный (#000000 или #0a0a0a)
- **Акцент 1**: Бирюзовый (#00D9FF) - для кнопок, ссылок, важных элементов
- **Акцент 2**: Золотой (#FFD700) - для премиум функций, больших сумм
- **Успех**: Зеленый (#00FF88) - для успешных операций
- **Ошибка**: Красный (#FF4444) - для ошибок, отклонений
- **Текст основной**: Белый (#FFFFFF)
- **Текст вторичный**: Серый (#CCCCCC)
- **Разделители**: Темно-серый (#333333)

**Типографика:**
- **Заголовки H1**: Bold, 28-32px
- **Заголовки H2**: Semibold, 24px
- **Заголовки H3**: Semibold, 20px
- **Основной текст**: Regular, 16px
- **Вторичный текст**: Regular, 14px
- **Мелкий текст**: Regular, 12px

**Отступы и размеры:**
- Базовый отступ: 16px
- Малый отступ: 8px
- Большой отступ: 24px
- Скругление углов: 8-12px
- Высота кнопок: 48-56px

### 9.2 Компоненты UI

**Карточки:**
- Белый/темно-серый фон
- Тень/обводка для выделения
- Скругленные углы
- Отступы внутри

**Кнопки:**
- Primary: Бирюзовый фон, белый текст
- Secondary: Прозрачный фон, бирюзовый текст, обводка
- Outline: Прозрачный фон, белый текст, обводка
- Destructive: Красный фон (для опасных действий)

**Поля ввода:**
- Темный фон
- Бирюзовый акцент при фокусе
- Плейсхолдер серого цвета
- Подсказки под полем

**Badge (статусы):**
- Зеленый - выполнено/успешно
- Желтый - в процессе/ожидание
- Красный - отклонено/ошибка
- Синий - новая информация

### 9.3 Навигация

**Структура навигации:**

**Для неавторизованных пользователей:**
```
AuthNavigator (Stack)
├── LoginScreen
├── RegisterScreen
└── VerificationScreen
```

**Для исполнителей:**
```
MainNavigator (Tab Navigator)
├── Home Tab (AvailableOrdersScreen)
├── Active Tab (ActiveOrdersScreen)
├── History Tab (HistoryScreen)
├── Stats Tab (StatisticsScreen)
└── Profile Tab (ProfileScreen)
```

**Для заказчиков:**
```
MainNavigator (Tab Navigator)
├── Create Tab (CreateOrderScreen)
├── Orders Tab (MyOrdersScreen)
├── Balance Tab (BalanceScreen)
└── Profile Tab (ProfileScreen)
```

**Stack навигация внутри каждого раздела:**
- При нажатии на элемент - переход на детальный экран
- Кнопка "Назад" в заголовке

---

## 📱 10. СПЕЦИФИЧЕСКИЕ ФУНКЦИИ ДЛЯ МОБИЛЬНОГО

### 10.1 Работа с камерой и галереей

**Библиотеки:**
- `expo-image-picker` - выбор изображений из галереи и камеры
- `expo-image-manipulator` - кроп, ресайз, сжатие изображений

**Функционал:**
- Выбор изображения из галереи
- Создание фото через камеру
- Автоматическое сжатие до 2MB
- Опциональный кроп изображения
- Предпросмотр перед загрузкой

### 10.2 Push-уведомления

**Библиотеки:**
- `expo-notifications` - для push-уведомлений
- `expo-device` - для получения device token

**Типы уведомлений:**
- Новое задание доступно (для исполнителей)
- Задание принято/отклонено (для заказчиков)
- Скриншот принят/отклонен (для исполнителей)
- Выплата получена
- Баланс пополнен
- Новый уровень достигнут

**Настройки:**
- Разрешение на отправку уведомлений при первом запуске
- Настройки в профиле (вкл/выкл)

### 10.3 Deep Links

**Поддержка ссылок:**
- `mbtrust://orders/[id]` - открытие конкретного заказа
- `mbtrust://payments/[id]` - открытие платежа
- `mbtrust://verification/telegram` - открытие Telegram верификации
- `mbtrust://profile` - открытие профиля

**Использование:**
- Для перехода из веб-версии в мобильное приложение
- Для открытия из email/SMS уведомлений
- Для Telegram бота (открытие в приложении)

**Библиотека:** `expo-linking`

### 10.4 QR-код сканер

**Для исполнителей:**
- Сканирование QR-кода задания для быстрого принятия
- Отображение QR-кода задания для публикации

**Библиотека:** `expo-barcode-scanner` или `react-native-qrcode-scanner`

### 10.5 Биометрическая аутентификация

**Опциональная функция:**
- Вход по отпечатку пальца (Android) / Face ID (iOS)
- Безопасное хранение токенов в Keychain/Keystore

**Библиотека:** `expo-local-authentication`

### 10.6 Офлайн режим (опционально)

**Функционал:**
- Кэширование списка заданий
- Локальное сохранение прогресса
- Синхронизация при появлении интернета
- Индикатор офлайн режима

**Библиотеки:**
- `@react-native-async-storage/async-storage` - локальное хранилище
- `@react-native-community/netinfo` - проверка интернета

---

## 🧪 11. ТЕСТИРОВАНИЕ

### 11.1 Типы тестов

**Unit тесты:**
- Тестирование утилит (форматирование, валидация)
- Тестирование API сервисов (моки)
- Инструмент: Jest

**Integration тесты:**
- Тестирование компонентов с состоянием
- Тестирование навигации
- Инструмент: React Native Testing Library

**E2E тесты:**
- Полные сценарии использования
- Автоматизация на реальных устройствах
- Инструмент: Detox или Appium

### 11.2 Тестовые сценарии

**Критические пути:**
1. Регистрация нового пользователя
2. Вход в приложение
3. Принятие задания (исполнитель)
4. Загрузка скриншота
5. Создание заказа (заказчик)
6. Пополнение баланса
7. Запрос выплаты

**Тестирование на устройствах:**
- Android: различные версии (6.0+), разные размеры экранов
- iOS: различные версии (12.0+), iPhone и iPad
- Тестирование на реальных устройствах перед релизом

---

## 🚀 12. РАЗВЕРТЫВАНИЕ И ПУБЛИКАЦИЯ

### 12.1 Подготовка к релизу

**Android:**
1. Создание keystore для подписи приложения
2. Настройка `app.json` (версия, название, иконка)
3. Сборка APK/AAB через Expo EAS Build

**iOS:**
1. Apple Developer аккаунт ($99/год)
2. Создание App ID и сертификатов
3. Настройка `app.json`
4. Сборка через Expo EAS Build

### 12.2 Сборка приложения

**Через Expo EAS Build:**
```bash
# Установка EAS CLI
npm install -g eas-cli

# Логин в Expo
eas login

# Настройка проекта
eas build:configure

# Сборка для Android
eas build --platform android --profile production

# Сборка для iOS
eas build --platform ios --profile production
```

**Локальная сборка (опционально):**
```bash
# Android
eas build --platform android --local

# iOS (только на macOS)
eas build --platform ios --local
```

### 12.3 Публикация в магазины

**Google Play Store:**
1. Создание приложения в Google Play Console
2. Загрузка AAB файла
3. Заполнение описания, скриншотов
4. Настройка цены и доступности
5. Отправка на проверку

**Apple App Store:**
1. Создание приложения в App Store Connect
2. Загрузка IPA файла (через Xcode или Transporter)
3. Заполнение метаданных
4. Настройка цены
5. Отправка на проверку Apple

### 12.4 OTA обновления (Over-The-Air)

**Expo Updates:**
- Обновление JavaScript кода без пересборки
- Быстрое исправление багов
- Новые функции без публикации в магазинах

**Команды:**
```bash
# Публикация обновления
eas update --branch production --message "Bug fixes"

# Откат обновления (если ошибка)
eas update --branch production --rollback
```

---

## 📋 13. ЧЕКЛИСТ РАЗРАБОТКИ

### Этап 1: Настройка проекта (Неделя 1)
- [ ] Создать React Native (Expo) проект
- [ ] Настроить TypeScript
- [ ] Настроить ESLint и Prettier
- [ ] Настроить навигацию (React Navigation)
- [ ] Настроить State Management (Zustand/Redux)
- [ ] Создать базовый API клиент
- [ ] Настроить переменные окружения

### Этап 2: Аутентификация (Неделя 2)
- [ ] Экран входа
- [ ] Экран регистрации (многошаговый)
- [ ] Сохранение токена в AsyncStorage
- [ ] Автоматический refresh токена
- [ ] Защита роутов (проверка авторизации)
- [ ] Экран верификации

### Этап 3: Профиль (Неделя 2-3)
- [ ] Экран профиля
- [ ] Редактирование профиля
- [ ] Настройки приложения

### Этап 4: Заказы для исполнителей (Неделя 3-4)
- [ ] Список доступных заданий
- [ ] Карточки заданий
- [ ] Фильтры и сортировка
- [ ] Принятие задания
- [ ] Список активных заданий
- [ ] Загрузка скриншота (камера/галерея)
- [ ] История заданий

### Этап 5: Заказы для заказчиков (Неделя 4-5)
- [ ] Создание заказа (многошаговая форма)
- [ ] Калькулятор стоимости
- [ ] Список моих заказов
- [ ] Детали заказа
- [ ] Модерация выполнений

### Этап 6: Платежи (Неделя 5-6)
- [ ] Экран баланса
- [ ] Пополнение баланса (интеграция платежей)
- [ ] Запрос выплаты
- [ ] История транзакций

### Этап 7: Дополнительные функции (Неделя 6-7)
- [ ] Push-уведомления
- [ ] Статистика и графики
- [ ] QR-код сканер
- [ ] Deep Links
- [ ] Офлайн режим (опционально)

### Этап 8: Тестирование (Неделя 7-8)
- [ ] Unit тесты
- [ ] Тестирование на эмуляторах
- [ ] Тестирование на реальных устройствах
- [ ] Исправление багов
- [ ] Тестирование производительности

### Этап 9: Подготовка к релизу (Неделя 8)
- [ ] Создание иконки приложения
- [ ] Splash screen
- [ ] Финальная проверка всех функций
- [ ] Подготовка скриншотов для магазинов
- [ ] Написание описания приложения

### Этап 10: Релиз (Неделя 9)
- [ ] Сборка production версий
- [ ] Публикация в Google Play (бета тестирование)
- [ ] Публикация в App Store (TestFlight)
- [ ] Сбор обратной связи
- [ ] Исправление критических багов
- [ ] Финальная публикация

---

## ✅ 14. КРИТЕРИИ ПРИЕМКИ

### 14.1 Функциональность
- ✅ Все основные функции работают без ошибок
- ✅ Нет критических багов, блокирующих использование
- ✅ Приложение стабильно работает на Android 6.0+ и iOS 12.0+

### 14.2 Производительность
- ✅ Время загрузки экранов < 2 секунд
- ✅ Плавная анимация (60 FPS)
- ✅ Оптимизированное потребление батареи
- ✅ Разумное использование памяти

### 14.3 Безопасность
- ✅ Безопасное хранение токенов (не в plain text)
- ✅ HTTPS для всех API запросов
- ✅ Валидация всех входных данных
- ✅ Защита от SQL injection, XSS (на стороне API)

### 14.4 UX/UI
- ✅ Интуитивно понятный интерфейс
- ✅ Соответствие дизайн-системе
- ✅ Поддержка темной темы (основная тема)
- ✅ Адаптивность под разные размеры экранов
- ✅ Доступность (accessibility)

### 14.5 Совместимость
- ✅ Работа на Android 6.0+ (API 23+)
- ✅ Работа на iOS 12.0+
- ✅ Поддержка различных размеров экранов
- ✅ Работа в портретной и ландшафтной ориентации (где необходимо)

---

## 📚 15. РЕСУРСЫ И ДОКУМЕНТАЦИЯ

### 15.1 Полезные ссылки
- [React Native Docs](https://reactnative.dev/)
- [Expo Documentation](https://docs.expo.dev/)
- [React Navigation](https://reactnavigation.org/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)

### 15.2 Рекомендуемые библиотеки
- **Навигация**: `@react-navigation/native`
- **Хранение**: `@react-native-async-storage/async-storage`
- **HTTP клиент**: `axios`
- **State Management**: `zustand` или `@reduxjs/toolkit`
- **Формы**: `react-hook-form`
- **Камера**: `expo-image-picker`
- **Уведомления**: `expo-notifications`
- **Графики**: `victory-native` или `react-native-chart-kit`

---

## 🔄 16. ИНТЕГРАЦИЯ С БЭКЕНДОМ

### 16.1 Что нужно сделать на бэкенде

**1. Создать endpoint для мобильного логина:**
```
POST /api/auth/mobile/login
```

**2. Добавить проверку Bearer токенов в middleware или в каждом API route:**
- Проверять заголовок `Authorization: Bearer <token>`
- Валидировать JWT токен
- Извлекать userId из токена

**3. Создать endpoint для обновления токена:**
```
POST /api/auth/mobile/refresh
```

**4. Настроить CORS (если нужно):**
- Разрешить запросы с домена мобильного приложения
- Или использовать `Access-Control-Allow-Origin: *` для разработки

### 16.2 Примеры кода для бэкенда

**Создание endpoint для мобильного логина:**
```typescript
// src/app/api/auth/mobile/login/route.ts
import { NextRequest, NextResponse } from 'next/server';
import jwt from 'jsonwebtoken';
import { prisma } from '@/lib/prisma';

export async function POST(request: NextRequest) {
  const { phone, password } = await request.json();
  
  const user = await prisma.user.findUnique({
    where: { phone }
  });
  
  if (!user) {
    return NextResponse.json({ error: 'Неверные данные' }, { status: 401 });
  }
  
  // Проверка пароля (если добавите проверку паролей)
  // const isValid = await bcrypt.compare(password, user.passwordHash);
  
  // Генерация JWT токена
  const token = jwt.sign(
    { 
      userId: user.id, 
      role: user.role,
      level: user.level 
    },
    process.env.NEXTAUTH_SECRET!,
    { expiresIn: '30d' }
  );
  
  return NextResponse.json({ 
    token,
    user: {
      id: user.id,
      name: user.name,
      phone: user.phone,
      email: user.email,
      role: user.role,
      level: user.level,
      balance: user.balance
    }
  });
}
```

**Middleware для проверки токена:**
```typescript
// src/lib/mobileAuth.ts
import jwt from 'jsonwebtoken';

export function getUserIdFromToken(request: NextRequest): string | null {
  const authHeader = request.headers.get('Authorization');
  
  if (!authHeader?.startsWith('Bearer ')) {
    return null;
  }
  
  const token = authHeader.substring(7);
  
  try {
    const decoded = jwt.verify(token, process.env.NEXTAUTH_SECRET!) as any;
    return decoded.userId;
  } catch (error) {
    return null;
  }
}
```

**Использование в API routes:**
```typescript
// Пример: src/app/api/orders/route.ts
import { getServerSession } from 'next-auth/next';
import { getUserIdFromToken } from '@/lib/mobileAuth';
import { authOptions } from '@/lib/auth';

export async function GET(request: NextRequest) {
  // Проверяем авторизацию (браузер или мобильное)
  let userId: string | null = null;
  
  // Вариант 1: NextAuth сессия (браузер)
  const session = await getServerSession(authOptions);
  if (session?.user?.id) {
    userId = session.user.id;
  }
  
  // Вариант 2: Bearer токен (мобильное)
  if (!userId) {
    userId = getUserIdFromToken(request);
  }
  
  if (!userId) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  // Дальше обычная логика
  const orders = await prisma.order.findMany({
    where: { customerId: userId }
  });
  
  return NextResponse.json(orders);
}
```

---

## 📝 ЗАКЛЮЧЕНИЕ

Это техническое задание описывает полную функциональность мобильного приложения MB-TRUST. Приложение является клиентом для существующего Next.js API и синхронизируется с ним автоматически через API запросы.

**Основные принципы:**
1. Мобильное приложение - это UI клиент
2. Вся бизнес-логика на бэкенде
3. Синхронизация автоматическая через API
4. Нужно добавить только мобильную аутентификацию (JWT токены)

**Следующие шаги:**
1. Создать проект в Android Studio / Cursor
2. Настроить базовую структуру
3. Интегрировать API (после добавления мобильной аутентификации на бэкенде)
4. Реализовать экраны по приоритетам
5. Тестировать и дорабатывать

**Версия документа:** 1.0  
**Дата создания:** 2024  
**Статус:** Готово к разработке

---

**Сделано с ❤️ для MB-TRUST**
