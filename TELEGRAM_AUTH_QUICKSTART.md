# 🚀 Telegram Авторизация - Быстрый Старт

## Что было сделано

✅ Добавлена **полноценная авторизация через Telegram**
✅ Если у пользователя привязан Telegram ID - он автоматически авторизуется
✅ Используется официальный Telegram Login Widget
✅ Полная проверка безопасности данных от Telegram

## 📝 Что нужно сделать для запуска

### 1. Добавить переменную окружения

Откройте `.env.local` и добавьте:

```env
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME="MBTRUST_bot"
```

### 2. Пересобрать и запустить

```bash
npm run build
npm start
```

### 3. Проверить

Откройте страницу входа: `http://localhost:3000/auth/signin`

Вы увидите кнопку **Telegram Login Widget** вместо старой кнопки.

## 🎯 Как это работает

1. **Пользователь нажимает на кнопку Telegram** → открывается окно авторизации Telegram
2. **Telegram возвращает данные пользователя** → проверяется подлинность через HMAC-SHA256
3. **Ищется пользователь с таким telegramId в БД** → если найден - создается сессия
4. **Пользователь авторизован** → перенаправляется на dashboard/executor

## ❌ Если Telegram не привязан

Пользователь увидит сообщение:
> "Telegram аккаунт не привязан к учетной записи. Пожалуйста, зарегистрируйтесь."

**Решение:** Пройти регистрацию через существующую систему с Telegram ботом.

## 📁 Созданные файлы

```
src/
├── app/
│   └── api/
│       └── auth/
│           └── telegram/
│               └── route.ts           # API endpoint для Telegram OAuth
└── components/
    └── auth/
        └── TelegramLoginButton.tsx    # Компонент кнопки Telegram
```

## 🔧 Изменённые файлы

```
src/lib/auth.ts                        # Добавлен Telegram provider
src/app/auth/signin/page.tsx          # Обновлена страница входа
next.config.ts                         # Добавлена публичная переменная
```

## 🔐 Безопасность

- ✅ Проверка подлинности данных от Telegram (HMAC-SHA256)
- ✅ Проверка времени авторизации (не старше 24 часов)
- ✅ Проверка блокировки пользователя
- ✅ Защита от CSRF через NextAuth
- ✅ Безопасное хранение в JWT

## 📋 Для деплоя на сервер

```bash
# 1. Добавить в .env.local
echo 'NEXT_PUBLIC_TELEGRAM_BOT_USERNAME="MBTRUST_bot"' >> .env.local

# 2. Пересобрать
npm run build

# 3. Перезапустить
sudo systemctl restart mb-trust

# 4. Проверить
curl http://localhost:3000/auth/signin
```

## 📖 Подробная документация

См. файл `TELEGRAM_AUTH_SETUP.md` для полной документации.

---

**Готово!** 🎉 Теперь пользователи могут входить через Telegram в один клик!

