# 🚀 Инструкция по развёртыванию изменений

## ✅ Что было реализовано

### 1. Telegram авторизация через генерацию кода
- Пользователь нажимает "Telegram" → видит сгенерированный код
- Может перейти по кнопке в бота ИЛИ скопировать код и отправить вручную
- После верификации в боте → автоматическая авторизация

### 2. Улучшенный дизайн карточек заданий
- Ссылка на ресурс (targetUrl)
- Все элементы эргономично расположены
- Дата размещения, локация, соцсеть, дедлайн
- Улучшенная визуальная иерархия

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ НА СЕРВЕРЕ

### Шаг 1: Загрузить файлы

```bash
cd /var/www/mb-trust

# Загрузите все обновлённые файлы через SFTP:
# - src/components/auth/TelegramAuthFlow.tsx (НОВЫЙ)
# - src/app/api/verification/telegram/check/route.ts (НОВЫЙ)
# - src/app/auth/signin/page.tsx (обновлён)
# - src/components/business/OrderCard.tsx (обновлён)
# - src/app/api/orders/route.ts (обновлён)
# - src/lib/ui-types.ts (обновлён)
# - prisma/schema.prisma (обновлён)
# - остальные исправленные файлы
```

### Шаг 2: Применить миграцию БД

```bash
cd /var/www/mb-trust

# Генерация Prisma Client
npx prisma generate

# Применение изменений к базе данных
npx prisma db push
```

**ВАЖНО:** Команда `prisma db push` добавит новое поле `targetUrl` в таблицу `orders`.
Все существующие заказы получат значение `NULL` для этого поля (это нормально).

### Шаг 3: Пересобрать проект

```bash
npm ci  # Установка зависимостей (если нужно)
npm run build
```

### Шаг 4: Перезапустить сервис

```bash
sudo systemctl restart mb-trust
```

### Шаг 5: Проверить логи

```bash
journalctl -u mb-trust -f
```

Должны появиться строки:
```
✓ Compiled successfully
Server ready on http://...
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Telegram авторизация

```bash
# 1. Откройте в браузере
http://109.69.58.185:3000/auth/signin

# 2. Нажмите кнопку "Telegram"

# 3. Должен появиться экран с:
- Большим кодом (например: A3F5B2)
- Кнопкой "Открыть Telegram бота"
- Кнопкой "Скопировать код"
- Инструкцией из 3 шагов

# 4. Скопируйте код или нажмите кнопку

# 5. Отправьте код боту в Telegram

# 6. Ответьте на вопросы бота (если требуется)

# 7. Вернитесь на страницу
# Должна произойти автоматическая авторизация и редирект
```

### Тест 2: UI карточек заданий

```bash
# 1. Авторизуйтесь как заказчик

# 2. Создайте задание с заполнением поля "Целевая ссылка"

# 3. Откройте /dashboard/customer

# 4. Проверьте карточку задания:
✅ Кнопка удаления - маленькая, справа вверху
✅ Статус - слева вверху (если есть)
✅ Название и описание
✅ Цена справа
✅ Ссылка на ресурс (кликабельная, с иконкой 🔗)
✅ Метаинформация: соцсеть, локация, дедлайн, дата
✅ Изображения в 2 колонки (если есть)

# 5. Откройте /executor/available (как исполнитель)

# 6. Проверьте, что все элементы отображаются корректно
```

---

## ⚠️ ВАЖНЫЕ МОМЕНТЫ

### 1. Поле targetUrl опционально
- Существующие заказы будут иметь `targetUrl = NULL`
- Новые заказы могут иметь или не иметь это поле
- Если targetUrl есть - отображается в карточке
- Если нет - просто не показывается

### 2. Telegram бот должен быть запущен
- Для авторизации через код нужен работающий бот
- Проверьте: `journalctl -u mb-trust | grep "Telegram бот успешно запущен"`
- Если бот не запущен: `curl http://localhost:3000/api/health`

### 3. Переменные окружения
Убедитесь, что в `.env.local` есть:
```env
TELEGRAM_BOT_TOKEN="8345454942:AAFoOh3asQ104UUvIxNgMTpLCwwFI1oIJDY"
TELEGRAM_BOT_USERNAME="MBTRUST_bot"
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME="MBTRUST_bot"
```

---

## 🐛 Типичные ошибки

### Ошибка: "Ошибка генерации кода"

**Причина:** Проблема с API `/api/verification/telegram/start`

**Решение:**
```bash
# Проверьте логи
journalctl -u mb-trust -f | grep "VERIFY-START"

# Должны быть строки:
[VERIFY-START] Запуск верификации для пользователя ...
[VERIFY-START] Сгенерирован код: ...
```

### Ошибка: "targetUrl not found"

**Причина:** Не применена миграция БД

**Решение:**
```bash
npx prisma db push
npx prisma generate
npm run build
```

### Ошибка: Оверлей не появляется

**Причина:** Не пересобран проект

**Решение:**
```bash
npm run build
sudo systemctl restart mb-trust
```

---

## ✅ Чеклист после деплоя

- [ ] Файлы загружены на сервер
- [ ] Prisma миграция применена (`npx prisma db push`)
- [ ] Проект пересобран (`npm run build`)
- [ ] Сервис перезапущен (`systemctl restart mb-trust`)
- [ ] Бот запущен (логи показывают "✅ Telegram бот успешно запущен")
- [ ] Страница `/auth/signin` открывается
- [ ] Кнопка "Telegram" работает → показывает экран с кодом
- [ ] Можно скопировать код
- [ ] Можно перейти в бота по кнопке
- [ ] После отправки кода боту → автоматическая авторизация
- [ ] Карточки заданий показывают ссылку на ресурс
- [ ] Кнопка удаления - маленькая, справа
- [ ] Оверлей появляется сверху

---

Всё готово к развёртыванию! 🎉

