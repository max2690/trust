# 🔗 Валидация и нормализация URL при создании заданий

**Дата:** 15 ноября 2025  
**Статус:** ✅ Реализовано

---

## 🎯 **Что было добавлено:**

### 1. **Автоматическая нормализация URL**

Система автоматически добавляет `https://` к ссылкам без протокола:

```typescript
// Пользователь вводит:
example.com
→ Система использует: https://example.com

// Пользователь вводит:
http://example.com
→ Система использует: http://example.com (без изменений)
```

### 2. **Валидация в реальном времени**

При вводе URL система сразу показывает:
- ✅ **Зеленая галочка** - ссылка корректна
- ❌ **Красный крестик** - ссылка некорректна

### 3. **Визуальная обратная связь**

**Корректная ссылка:**
```
✅ Корректная ссылка
   Будет использована: https://example.com
```

**Некорректная ссылка:**
```
❌ Некорректный формат URL
   Примеры: example.com, https://example.com/page
```

### 4. **Блокировка некорректных ссылок**

Кнопка "Создать задание" становится неактивной, если:
- URL не валидный
- URL указывает на localhost или локальные адреса

### 5. **Улучшенное превью изображений**

Добавлен отдельный блок с превью:
- Обработанное изображение с QR-кодом
- Отдельный QR-код для проверки

---

## 📋 **Правила валидации:**

### ✅ **Допустимые форматы:**

```
example.com
www.example.com
example.com/page
example.com/page?query=value
http://example.com
https://example.com
https://example.com:8080/path
subdomain.example.com
example.com/path/to/page#anchor
```

### ❌ **Недопустимые форматы:**

```
localhost
127.0.0.1
192.168.1.1
example (без домена верхнего уровня)
just text
```

---

## 🛠 **Технические детали:**

### Утилита валидации: `src/lib/url-utils.ts`

**Функции:**

1. **`normalizeUrl(url: string): string`**
   - Добавляет `https://` если отсутствует протокол
   - Удаляет лишние пробелы

2. **`validateAndNormalizeUrl(url: string)`**
   - Возвращает объект с:
     - `normalized` - нормализованный URL
     - `isValid` - булево значение валидности
     - `error` - текст ошибки (если есть)

3. **`isValidUrl(url: string): boolean`**
   - Проверяет валидность через встроенный `URL` конструктор

4. **`getDomainFromUrl(url: string): string`**
   - Извлекает домен из URL

5. **`formatUrlForDisplay(url: string): string`**
   - Форматирует URL для отображения (убирает протокол)

---

## 📊 **Примеры использования:**

### Пример 1: Ввод без протокола

```typescript
Ввод: "example.com"
Результат:
{
  normalized: "https://example.com",
  isValid: true
}
```

### Пример 2: Ввод с протоколом

```typescript
Ввод: "http://example.com/page"
Результат:
{
  normalized: "http://example.com/page",
  isValid: true
}
```

### Пример 3: Некорректный ввод

```typescript
Ввод: "localhost"
Результат:
{
  normalized: "https://localhost",
  isValid: false,
  error: "Локальные адреса не допускаются"
}
```

### Пример 4: Невалидный URL

```typescript
Ввод: "just text"
Результат:
{
  normalized: "https://just text",
  isValid: false,
  error: "Некорректный формат URL. Пример: example.com или https://example.com"
}
```

---

## 🎨 **UI компоненты:**

### 1. **Поле ввода с валидацией**

```tsx
<Input
  value={formData.targetUrl}
  onChange={(e) => setFormData({ ...formData, targetUrl: e.target.value })}
  placeholder="example.com или https://example.com"
  className={
    urlValidation.isValid 
      ? 'border-green-500' 
      : 'border-red-500'
  }
/>
```

### 2. **Индикатор валидации**

```tsx
{urlValidation.isValid ? (
  <CheckCircle className="text-green-500" />
) : (
  <AlertCircle className="text-red-500" />
)}
```

### 3. **Превью изображений**

Показывается после загрузки файла:
```tsx
<div className="grid grid-cols-2 gap-4">
  <div>
    <p>Изображение с QR-кодом:</p>
    <img src={processedImageUrl} />
  </div>
  <div>
    <p>QR-код:</p>
    <img src={qrCodeUrl} />
  </div>
</div>
```

---

## 🔍 **Проверка работы:**

### Тест 1: Ввод простого домена
```
1. Откройте страницу создания задания
2. Введите "google.com" в поле "Целевая ссылка"
3. Убедитесь что появилась зеленая галочка
4. Проверьте текст: "Будет использована: https://google.com"
```

### Тест 2: Ввод с протоколом
```
1. Введите "http://example.com/page"
2. Убедитесь что появилась зеленая галочка
3. URL остался без изменений
```

### Тест 3: Ввод невалидного URL
```
1. Введите "localhost"
2. Убедитесь что появился красный крестик
3. Кнопка "Создать задание" неактивна
4. Показано сообщение об ошибке
```

### Тест 4: Загрузка изображения
```
1. Загрузите изображение
2. Дождитесь обработки
3. Убедитесь что отображается:
   - Оригинальное изображение
   - Обработанное изображение с QR-кодом
   - Отдельный QR-код
   - Блок "✅ Изображения готовы к публикации"
```

---

## 🐛 **Обработка ошибок:**

### Ошибка 1: URL не может быть пустым
```
Условие: Пользователь не ввел URL
Реакция: Поле required, форма не отправляется
```

### Ошибка 2: Некорректный формат URL
```
Условие: URL не проходит валидацию
Реакция: 
- Красная рамка вокруг поля
- Иконка AlertCircle
- Сообщение об ошибке
- Кнопка неактивна
```

### Ошибка 3: Локальный адрес
```
Условие: URL указывает на localhost/127.0.0.1/192.168.x.x
Реакция:
- Ошибка "Локальные адреса не допускаются"
- Кнопка неактивна
```

---

## 📝 **Измененные файлы:**

1. ✅ `src/lib/url-utils.ts` - утилиты для работы с URL (новый файл)
2. ✅ `src/app/dashboard/customer/create-order/page.tsx` - форма создания заказа
   - Добавлена валидация URL
   - Добавлен визуальный feedback
   - Добавлено улучшенное превью
   - Добавлена блокировка некорректных ссылок

---

## 🎯 **Преимущества:**

1. **UX:** Пользователь сразу видит, что ссылка корректна
2. **Безопасность:** Блокировка локальных адресов
3. **Удобство:** Автоматическое добавление https://
4. **Визуализация:** Превью QR-кода и изображения
5. **Валидация:** Предотвращение ошибок до отправки

---

## 🔮 **Возможные улучшения:**

1. Проверка доступности URL (ping)
2. Предупреждение о коротких ссылках
3. Поддержка UTM-меток
4. Генерация коротких ссылок
5. История использованных URL

---

**Валидация URL работает стабильно! ✅**

