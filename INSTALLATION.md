# 🚀 УСТАНОВКА ЗАЩИТЫ БОТА — ПОШАГОВАЯ ИНСТРУКЦИЯ

## 📋 ЧТО ИЗМЕНИЛОСЬ

### Обновлённые файлы:
1. ✅ `src/lib/bot-launcher.ts` — улучшенная защита от конфликтов, health check, graceful shutdown
2. ✅ `src/lib/telegram-init.ts` — rate limiting, валидация, автоочистка сессий, retry для OpenAI
3. ✅ `mb-trust-production.service` — systemd unit с автозапуском бота и watchdog

### Новые файлы:
4. ✅ `PROTECTION_GUIDE.md` — полное описание всех защит
5. ✅ `INSTALLATION.md` — эта инструкция

---

## 🔧 УСТАНОВКА (КОПИРОВАНИЕ ВРУЧНУЮ)

### Шаг 1: Скопировать файлы на сервер

На вашем **локальном компьютере** выполните:

```bash
# Скопировать обновлённые файлы
scp src/lib/bot-launcher.ts root@109.69.58.185:/var/www/mb-trust/src/lib/bot-launcher.ts
scp src/lib/telegram-init.ts root@109.69.58.185:/var/www/mb-trust/src/lib/telegram-init.ts
scp mb-trust-production.service root@109.69.58.185:/tmp/mb-trust-production.service

# Скопировать документацию
scp PROTECTION_GUIDE.md root@109.69.58.185:/var/www/mb-trust/
scp INSTALLATION.md root@109.69.58.185:/var/www/mb-trust/
```

---

### Шаг 2: На сервере — установить systemd unit

Подключитесь к серверу:

```bash
ssh root@109.69.58.185
```

Затем выполните:

```bash
# Установить новый systemd unit
sudo cp /tmp/mb-trust-production.service /etc/systemd/system/mb-trust.service

# Перезагрузить конфигурацию systemd
sudo systemctl daemon-reload

# Проверить, что файл загружен
systemctl cat mb-trust
```

Вы должны увидеть содержимое нового unit файла с `ExecStartPost` строками.

---

### Шаг 3: Пересобрать проект

```bash
cd /var/www/mb-trust

# Пересобрать проект с новыми изменениями
npm run build
```

**Ожидаемый вывод:**
```
✓ Creating an optimized production build
✓ Compiled successfully
```

**НЕ ДОЛЖНО быть:**
- ❌ `ETELEGRAM: 409 Conflict`
- ❌ `EACCES` ошибок

Если есть ошибки доступа:
```bash
sudo chown -R www-data:www-data /var/www/mb-trust/.next
sudo chmod -R 755 /var/www/mb-trust/.next
npm run build
```

---

### Шаг 4: Перезапустить сервис

```bash
# Перезапустить сервис с новой конфигурацией
sudo systemctl restart mb-trust

# Следить за логами в реальном времени
journalctl -u mb-trust -f
```

---

### Шаг 5: Проверка успешной установки

**Через 5-10 секунд после рестарта** вы должны увидеть в логах:

```
Nov 11 XX:XX:XX mbimport.ru npx[XXXXX]:   ▲ Next.js 13.5.11
Nov 11 XX:XX:XX mbimport.ru npx[XXXXX]:   - Local:        http://localhost:3000
Nov 11 XX:XX:XX mbimport.ru npx[XXXXX]:  ✓ Ready in 973ms
Nov 11 XX:XX:XX mbimport.ru npx[XXXXX]: 🤖 Запуск Telegram бота...
Nov 11 XX:XX:XX mbimport.ru npx[XXXXX]: ✅ Telegram бот успешно запущен
```

**✅ Если видите эти строки — всё установлено корректно!**

**❌ Если НЕ видите "🤖 Запуск Telegram бота...":**

```bash
# Вызовите health check вручную
curl http://localhost:3000/api/health

# Проверьте логи снова
journalctl -u mb-trust -n 20
```

---

## 🧪 ТЕСТИРОВАНИЕ

### 1. Проверка автозапуска бота

```bash
# Перезапустить сервис
sudo systemctl restart mb-trust

# Подождать 5 секунд
sleep 5

# Проверить, что бот запустился
journalctl -u mb-trust | grep "Telegram бот успешно запущен"
```

**Ожидается:** Строка с "✅ Telegram бот успешно запущен"

---

### 2. Тест регистрации

1. Откройте сайт: http://109.69.58.185:3000
2. Перейдите к регистрации исполнителя
3. Нажмите "Открыть Telegram бота"
4. В боте отправьте `/start` с кодом
5. Ответьте на все вопросы бота

**Ожидается:**
- Бот отвечает быстро
- Все данные собираются корректно
- В конце: "✅ Отлично! Все данные собраны. Твой аккаунт привязан и верифицирован."
- Статус на сайте обновился на "Верифицирован"

---

### 3. Тест защиты от спама

В боте отправьте **15 сообщений подряд** (можно просто "тест" 15 раз).

**Ожидается:**
- Первые 10 сообщений обрабатываются
- 11-15 сообщения: "⚠️ Слишком много сообщений. Пожалуйста, подожди немного"

**Проверка логов:**
```bash
journalctl -u mb-trust | grep "Rate limit превышен"
```

---

### 4. Тест валидации

Отправьте боту мусорное сообщение:

```
aaaaaaaaaaaaaaaaaaaaaaaaa
```

**Ожидается:**
```
⚠️ Обнаружен спам (повторяющиеся символы). Пожалуйста, отправь нормальное текстовое сообщение.
```

**Проверка логов:**
```bash
journalctl -u mb-trust | grep "Невалидное сообщение"
```

---

### 5. Тест автоперезапуска

```bash
# Убить процесс
sudo pkill -9 -f "npm start"

# Подождать 10 секунд
sleep 10

# Проверить статус
systemctl status mb-trust
```

**Ожидается:**
```
Active: active (running)
```

Systemd автоматически перезапустил сервис!

---

## 📊 МОНИТОРИНГ

### Проверка логов:

```bash
# Все логи в реальном времени
journalctl -u mb-trust -f

# Только логи бота
journalctl -u mb-trust -f | grep -E "🤖|BOT|SPAM|CLEANUP"

# Последние 50 строк
journalctl -u mb-trust -n 50

# Логи за последний час
journalctl -u mb-trust --since "1 hour ago"
```

### Статистика защиты:

```bash
# Сколько раз блокировали спам
journalctl -u mb-trust | grep "Rate limit" | wc -l

# Сколько сессий очищено
journalctl -u mb-trust | grep "CLEANUP" | wc -l

# Сколько OpenAI retry
journalctl -u mb-trust | grep "Попытка 2/3" | wc -l
```

---

## ✅ ЧЕКЛИСТ ПОСЛЕ УСТАНОВКИ

- [ ] `npm run build` прошёл без ошибок
- [ ] `sudo systemctl restart mb-trust` выполнен
- [ ] В логах есть "🤖 Запуск Telegram бота..."
- [ ] В логах есть "✅ Telegram бот успешно запущен"
- [ ] `curl http://localhost:3000/api/health` возвращает `{"ok":true}`
- [ ] Регистрация в боте работает от начала до конца
- [ ] Тест спама блокирует 11-15 сообщения
- [ ] Тест валидации блокирует мусор
- [ ] `systemctl status mb-trust` показывает `active (running)`

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проблема: Бот не запускается

```bash
# Проверьте переменные окружения
cat /var/www/mb-trust/.env.local | grep TELEGRAM_BOT_TOKEN

# Должно быть:
# TELEGRAM_BOT_TOKEN="8345454942:..."

# Вызовите health check
curl http://localhost:3000/api/health

# Проверьте логи
journalctl -u mb-trust -n 30 | grep -E "🤖|⚠️|❌"
```

### Проблема: 409 Conflict

```bash
# Остановите всё
sudo systemctl stop mb-trust

# Подождите 5 секунд
sleep 5

# Запустите снова
sudo systemctl start mb-trust

# Следите за логами
journalctl -u mb-trust -f
```

### Проблема: npm run build падает

```bash
# Проверьте права доступа
ls -la /var/www/mb-trust/.next

# Исправьте права
sudo chown -R www-data:www-data /var/www/mb-trust
sudo chmod -R 755 /var/www/mb-trust

# Попробуйте снова
npm run build
```

---

## 🎯 БЫСТРЫЕ КОМАНДЫ

```bash
# Полный перезапуск и проверка
sudo systemctl restart mb-trust && sleep 5 && journalctl -u mb-trust -n 20 | grep "🤖"

# Проверка здоровья
curl http://localhost:3000/api/health && echo ""

# Мониторинг спама
journalctl -u mb-trust -f | grep SPAM
```

---

## 📞 ПОДДЕРЖКА

Если возникли проблемы:

1. Сохраните логи:
```bash
journalctl -u mb-trust -n 200 > /tmp/mb-trust-logs.txt
```

2. Проверьте статус:
```bash
systemctl status mb-trust > /tmp/mb-trust-status.txt
```

3. Отправьте эти файлы для анализа

---

**🎉 УСТАНОВКА ЗАВЕРШЕНА!**

Теперь ваш бот защищён от:
- ✅ Конфликтов (409)
- ✅ Спама (rate limiting)
- ✅ Мусорных сообщений (валидация)
- ✅ Зависших сессий (автоочистка)
- ✅ Сбоев OpenAI (retry + fallback)
- ✅ Падений (автоперезапуск)

